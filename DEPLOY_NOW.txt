═══════════════════════════════════════════════════════════════
  EVIMERIA SYSTEM - PRODUCTION DEPLOYMENT GUIDE
═══════════════════════════════════════════════════════════════

📦 DEPLOYMENT PACKAGE READY:
   - Latest code pushed to GitHub (commit: 0c3d459)
   - Frontend built: frontend/dist.zip
   - All features tested and ready

═══════════════════════════════════════════════════════════════
  STEP 1: UPLOAD FRONTEND BUILD
═══════════════════════════════════════════════════════════════

Using FileZilla/WinSCP:
   Local:  D:\Projects\Evimeria_System\frontend\dist.zip
   Server: /home/1980royalce1/domains/evimeria.breysomsolutions.co.ke/public_html/

═══════════════════════════════════════════════════════════════
  STEP 2: SSH TO SERVER
═══════════════════════════════════════════════════════════════

Open PowerShell/Terminal and run:

   ssh 1980royalce1@breysomsolutions.co.ke

═══════════════════════════════════════════════════════════════
  STEP 3: RUN DEPLOYMENT SCRIPT
═══════════════════════════════════════════════════════════════

Copy and paste these commands one by one:

# Navigate to project
cd ~/domains/evimeria.breysomsolutions.co.ke/public_html

# Run deployment
bash deploy_quick.sh

# Enter sudo password when prompted (if required)

═══════════════════════════════════════════════════════════════
  STEP 4: DEPLOY FRONTEND
═══════════════════════════════════════════════════════════════

# Navigate to public folder
cd public

# Backup existing files
mkdir -p ../backups/$(date +%Y%m%d_%H%M%S)
mv index.html assets ../backups/$(date +%Y%m%d_%H%M%S)/ 2>/dev/null

# Extract new frontend
unzip -o ../dist.zip
rm ../dist.zip

# Verify deployment
ls -la

# You should see:
#   - index.html
#   - assets/

═══════════════════════════════════════════════════════════════
  STEP 5: GENERATE INVOICES (FIRST TIME ONLY)
═══════════════════════════════════════════════════════════════

cd ../backend

# Check current invoice count
php artisan tinker --execute="echo 'Invoices: ' . App\Models\Invoice::count() . PHP_EOL;"

# If count is 0 or low, generate invoices
php artisan invoices:backfill

# Verify generation
php artisan tinker --execute="echo 'Total Invoices: ' . App\Models\Invoice::count() . PHP_EOL; echo 'Sample member invoices: ' . App\Models\Member::find(25)->invoices()->count() . PHP_EOL;"

═══════════════════════════════════════════════════════════════
  STEP 6: SETUP CRON JOB (ONE TIME SETUP)
═══════════════════════════════════════════════════════════════

# Edit crontab
crontab -e

# Press 'i' to insert, then add this line:
* * * * * cd ~/domains/evimeria.breysomsolutions.co.ke/public_html/backend && php artisan schedule:run >> /dev/null 2>&1

# Press ESC, then type :wq and press ENTER to save

# Verify cron job
crontab -l

═══════════════════════════════════════════════════════════════
  STEP 7: VERIFY DEPLOYMENT
═══════════════════════════════════════════════════════════════

Visit: https://evimeria.breysomsolutions.co.ke

✅ Check List:
   □ Login page loads with logo
   □ Dashboard displays with new gradient design
   □ Charts show data (Invoice trends, Member status pie chart)
   □ Navigate to Invoices page - should show invoices
   □ Navigate to Bulk SMS - should have modern purple design
   □ Open a member profile - should show new fields (ID Number, Church)
   □ Try downloading a statement PDF - logo should appear
   □ Send test statement link to member - profile completion should work

═══════════════════════════════════════════════════════════════
  TROUBLESHOOTING
═══════════════════════════════════════════════════════════════

IF FRONTEND NOT LOADING:
   cd ~/domains/evimeria.breysomsolutions.co.ke/public_html/public
   ls -la
   # Should see index.html and assets folder

IF GETTING 500 ERRORS:
   cd ~/domains/evimeria.breysomsolutions.co.ke/public_html/backend
   tail -50 storage/logs/laravel.log

IF INVOICES NOT SHOWING:
   cd ~/domains/evimeria.breysomsolutions.co.ke/public_html/backend
   php artisan migrate:status
   php artisan cache:clear
   php artisan config:clear

IF LOGO NOT SHOWING:
   ls -la storage/app/public/settings/
   ls -la ../public/storage
   # If storage link missing:
   php artisan storage:link

═══════════════════════════════════════════════════════════════
  QUICK TEST COMMANDS
═══════════════════════════════════════════════════════════════

cd ~/domains/evimeria.breysomsolutions.co.ke/public_html/backend

# Check database connection
php artisan tinker --execute="DB::connection()->getPdo(); echo 'Database connected!' . PHP_EOL;"

# Check members count
php artisan tinker --execute="echo 'Members: ' . App\Models\Member::count() . PHP_EOL;"

# Check invoices count
php artisan tinker --execute="echo 'Invoices: ' . App\Models\Invoice::count() . PHP_EOL;"

# Check settings
php artisan tinker --execute="echo 'App Name: ' . App\Models\Setting::get('app_name') . PHP_EOL; echo 'Logo: ' . App\Models\Setting::get('logo_path') . PHP_EOL;"

# Clear all caches
php artisan cache:clear && php artisan config:clear && php artisan route:clear && php artisan view:clear

═══════════════════════════════════════════════════════════════
  ROLLBACK (IF NEEDED)
═══════════════════════════════════════════════════════════════

If deployment fails and you need to rollback:

# Rollback database
cd ~/domains/evimeria.breysomsolutions.co.ke/public_html/backend
php artisan migrate:rollback --step=2

# Restore frontend
cd ../public
rm -rf index.html assets
cp -r ../backups/TIMESTAMP/* .

# Restore code
cd ..
git reset --hard HEAD~3
composer install

═══════════════════════════════════════════════════════════════
  DEPLOYMENT SUMMARY
═══════════════════════════════════════════════════════════════

🎯 What's Being Deployed:
   ✅ Complete invoice system (52 weeks × KES 1,000 per member)
   ✅ Profile completion requirement for statements
   ✅ Modern gradient theme across all pages
   ✅ Enhanced Bulk SMS with full message display
   ✅ Revamped dashboard with interactive charts
   ✅ Fixed duplicate transaction detection
   ✅ Logo support in web views and PDFs
   ✅ New member fields (Church, ID Number, Alt Phone)

📊 Expected Results:
   - 13,572 invoices (234 members × 58 weeks)
   - Modern purple-pink gradient theme
   - Interactive charts on dashboard
   - Full SMS messages visible in logs
   - Members must complete profile before viewing statements

🔗 URLs:
   Frontend: https://evimeria.breysomsolutions.co.ke
   Backend:  https://evimeria.breysomsolutions.co.ke/api/v1/

═══════════════════════════════════════════════════════════════
  NEED HELP?
═══════════════════════════════════════════════════════════════

If you encounter issues:
1. Check Laravel logs: tail -f backend/storage/logs/laravel.log
2. Check web server logs: tail -f /var/log/apache2/error.log
3. Test database: php artisan tinker --execute="DB::connection()->getPdo();"
4. Verify .env file has correct settings

═══════════════════════════════════════════════════════════════
  🚀 READY TO DEPLOY!
═══════════════════════════════════════════════════════════════

All code is committed and pushed to GitHub.
Frontend is built and ready to upload.
Deployment script is tested and ready.

Follow Steps 1-7 above to complete deployment! 🎉

═══════════════════════════════════════════════════════════════

